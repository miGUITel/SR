(revisar capturas que puedo añadir y las que se les pide: yo creo
Captura de la configuración IP.
Captura del Administrador DNS.
Captura del nslookup localhost.)


# 🧪 **Práctica 4 — Instalación y puesta en marcha del servicio DNS en Windows Server 2019**

**Módulo:** Servicios en Red
**Unidad de Trabajo:** UT03 – DNS
**Sesión:** 4

---

## Objetivo de la práctica

Instalar el servicio DNS en **Windows Server 2019**, configurar correctamente la **red interna y la IP estática**, y comprobar que el **servicio DNS está operativo**, dejando el servidor preparado para la creación de zonas y registros en las siguientes sesiones.

En esta práctica **no se crean zonas DNS** ni se configuran servidores secundarios, subdominios o reenviadores.

---

## Escenario de trabajo

* Una máquina virtual con **Windows Server 2019**.
* VirtualBox.
* Red configurada en **modo Red Interna**.
* No se requiere conexión a Internet.

---

## Tareas a realizar

---

## 1. Configuración de red en VirtualBox (red interna)

Configura la tarjeta de red de la máquina virtual en **modo Red Interna**:

* VirtualBox → Configuración → Red → Adaptador 1 → **Red Interna**

📌 A partir de este momento, **no se cambiará el tipo de red** durante la práctica.

---

## 2. Configuración de IP estática en Windows Server

Configura una dirección IP fija en el servidor, acorde a la red interna que se utilizará durante toda la UT.

Ejemplo de configuración:

* Dirección IP: `172.16.0.4`
* Máscara: `255.255.0.0`
* Puerta de enlace: *(no necesaria en red interna)*
* DNS preferido: `172.16.0.4`

La configuración puede realizarse:

* de forma gráfica
* o mediante PowerShell

📸 **Captura 1 (obligatoria)**

* Propiedades de IPv4 mostrando la IP estática configurada.

---

## 3. Instalación del rol DNS

Instala el servicio DNS utilizando **PowerShell**:

```powershell
Install-WindowsFeature -Name DNS -IncludeManagementTools
```

Comprueba que la instalación finaliza correctamente.

📎 *Captura reutilizable de tu guion:*

```md
![alt text](../1.png)
```

*(Instalación del rol DNS en Windows Server)*

---

## 4. Comprobación del estado del servicio DNS

Comprueba que el servicio DNS está instalado y en ejecución:

```powershell
Get-Service -Name DNS
```

El servicio debe aparecer en estado **Running**.

📸 **Captura 4 (obligatoria)**

* Salida del comando `Get-Service -Name DNS` mostrando el servicio DNS en ejecución.

---

## 5. Primer acceso al Administrador DNS

Abre la consola **Administrador de DNS**:

* Herramientas administrativas → DNS

Observa:

* El nombre del servidor.
* La estructura inicial del servicio.
* Las carpetas de:

  * Zonas de búsqueda directa
  * Zonas de búsqueda inversa

📌 En este punto **no debe existir ninguna zona creada**.

📎 *Captura reutilizable de tu guion:*



*(Vista inicial del Administrador DNS en Windows Server)*

📸 **Captura 5 (obligatoria)**

* Vista del Administrador DNS sin zonas configuradas.

---

## Comprobación básica del servicio

Desde el propio servidor, ejecuta:

```powershell
nslookup localhost
```

Comprueba que:

* El servicio DNS responde.
* El servidor es capaz de atender consultas locales.

(No es necesario aportar captura de este paso).

---

## Instrucciones de entrega (importante)

* **Esta práctica es obligatoria**, pero **NO se entrega todavía**.
* Se entregará **junto con las siguientes prácticas de la UT**, también obligatorias.
* A partir de ahora:

  * Debes **guardar todas las capturas** solicitadas.
  * Debes **responder por escrito** a las preguntas que se planteen en cada práctica.
* Todo el material se recopilará en **una única memoria final** de la UT.

---

## Capturas que debes conservar de esta práctica

* 📸 Captura 1 → IP estática configurada
* 📸 Captura 4 → Servicio DNS en ejecución
* 📸 Captura 5 → Administrador DNS (estado inicial)

---

## Qué **no** se hace en esta práctica

❌ No se crean zonas
❌ No se crean registros
❌ No se configuran reenviadores
❌ No se clonan máquinas
❌ No se configuran servidores secundarios

---

## Idea clave que debe quedar clara

> *Antes de enseñar al DNS qué dominio controla,
> el servicio debe estar correctamente instalado,
> con una red estable y una IP bien definida.*

